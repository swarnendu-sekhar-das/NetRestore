[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "netrestore"
version = "0.1.0"
description = "Network Fault Restoration QA System"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
