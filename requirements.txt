# NetRestore System - Python Dependencies
# This file lists all Python package dependencies for the NetRestore system.
# Install with: pip install -r requirements.txt
# Or with uv (faster): uv pip install -r requirements.txt

# LlamaIndex Core - RAG Framework
llama-index==0.14.16
llama-index-embeddings-huggingface==0.7.0
llama-index-llms-groq==0.5.0
llama-index-vector-stores-chroma==0.5.5

# Vector Database Clients
chromadb==1.5.9                    # Primary vector store for production

# Retrieval & Reranking
rank-bm25==0.2.2                   # BM25 sparse retrieval for hybrid search
sentence-transformers==5.6.0       # Cross-encoder reranking model

# Web Application & API
streamlit==1.60.0                   # Main web application UI

# Document Processing
pypdf==6.14.2                       # PDF parsing for SOP documents
reportlab==5.0.0                   # PDF generation for synthetic SOP dataset
groq==1.5.0                        # Groq SDK for LLM-based SOP generation

# Development & Testing
jupyterlab==4.6.2                  # Interactive development environment
ragas==0.4.3                       # RAG evaluation metrics
pytest>=8.0.0                      # Unit testing framework

# Utilities
python-dotenv==1.2.2               # Environment variable management

# Dependency Constraints
# Note: torchvision is installed via Dockerfile CPU-only wheel index
# (--index-url pytorch.org). Do NOT add it here to avoid CUDA-heavy builds.
protobuf>=4.0.0,<6.0.0      # chromadb 0.4+ requires protobuf>=4; cap at 6 for safety
datasets>=2.20.0            # Required for evaluation and testing
